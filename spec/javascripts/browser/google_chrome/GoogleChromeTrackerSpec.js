describe('GoogleChromeTracker', function() {
  describe('.sendRequest', function() {
    //it('sends a message to the worker', function() {
      //spyOn(chrome.extension, 'sendRequest');
      //GoogleChromeTracker.sendRequest('mymessage', { color: 'purple' });
      //expect(chrome.extension.sendRequest).
        //toHaveBeenCalledWith({ action: 'mymessage', color: 'purple' });
    //});
  });
});
