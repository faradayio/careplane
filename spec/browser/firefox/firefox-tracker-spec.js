describe('FirefoxTracker', function() {
  describe('.sendRequest', function() {
    //it('sends a message to the worker', function() {
      //spyOn(self.port, 'emit');
      //FirefoxTracker.sendRequest('mymessage', { color: 'purple' });
      //expect(self.port.emit).toHaveBeenCalledWith({ action: 'mymessage', color: 'purple' });
    //});
  });
});
